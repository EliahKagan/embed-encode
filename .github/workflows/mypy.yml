name: mypy Typecheck

on: [push, pull_request]

jobs:
  typecheck:
    runs-on: ubuntu-latest
    name: Typecheck
    steps:
      - name: mypy Typecheck
        uses: jpetrucciani/mypy-check@master
        with:
          python_version: "3.11"
